# ClawHub API

Hono + Drizzle + PostgreSQL 16 + Redis 7 + tweetnacl. Serves the REST API **and** Git Smart HTTP on the same port.

## Entry points

- `src/index.ts` — instantiates `GitService` and `EventBus`, wires into `buildApp({ db, git, events })`, serves on `PORT`.
- `src/worker.ts` — standalone post-push worker. Drains the Redis Streams push queue + merge queue. Run with `npm -w @clawhub/api run dev:worker` (or `start:worker` in prod). The API process also runs an in-process worker by default; set `CLAWHUB_DISABLE_INPROC_WORKER=1` in prod to run workers standalone.
- `src/replication-worker.ts` — per-shard replication tailer. Reads `ref_log` for repos hosted on `$CLAWHUB_SHARD_ID` and applies them via the shard's gRPC/HTTP surface. Run one per replica shard.
- `src/backup-worker.ts` — periodic backup sweep. Iterates repos whose last backup is older than `CLAWHUB_BACKUP_INTERVAL_MS` (default 1h) and uploads manifests via the configured object store.
- `src/migrate.ts` — boot-time migrator (drizzle-orm programmatic migrate + Postgres advisory lock). The production container runs `node dist/migrate.js && node dist/index.js`; interchangeable with `npm run db:migrate`.
- `src/app.ts` — middleware + route mounting. LFS mounts before git-http at root — git-http's catch-all (`/:ns/:repo.git/*`) would otherwise swallow LFS paths.

## Route mount order

1. CORS (`*`)
2. **Git Smart HTTP at root** (`/:ns/:repo.git/*`) — owns its own auth via `authenticateGitRequest`.
3. Rate limiter on `/api/*`.
4. Public REST: `/api/v1/health`, `/api/v1/users` (login/register), `/api/v1/agents` (register), `/api/v1/ci/runs/:id` (runner callback).
5. Protected REST: everything else — uses `authMiddleware`, reads `tokenPayload` from context.
6. `app.onError(errorHandler)` maps `AppError` subclasses to HTTP codes.

## Git fetch auth

**Authentication is NOT access (#146).** The non-push branch of `routes/git-http.ts` authorizes the caller against the repo via `repoAccessFor(db, repo, callerFromGitAuth(auth))` — the same helper LFS and OCI use — before the `git http-backend` proxy and before shard forwarding, so both fetch entry points (`GET info/refs?service=git-upload-pack`, `POST git-upload-pack`) and the dumb-HTTP object paths are covered. It previously asked only "is the caller anonymous?" (`auth.kind !== "agent" && auth.kind !== "user"`), which any 10-second-old self-serve token satisfies; with repos private by default and `GIT_HTTP_EXPORT_ALL=1`, that made every private repo on the instance cloneable by anyone who could register — `gh-mirror` shadow repos of third-party GitHub private PRs included. Denials are split on purpose: **anonymous → 401 + `WWW-Authenticate`** (git probes without credentials first, so a 404 here would break `git clone` of a repo you own), **authenticated-but-unauthorized → 404** (mirrors `requireRepoRead`; no existence leak). **An allowlisted operator runner (`CLAWHUB_RUNNER_AGENT_IDS`) is admitted to FETCH any repo (#147)**: the dispatch gate (`mayReceiveRunDispatch`) hands the shared runner pool runs — plus each run's secrets — for every repo on the instance, and the runner clones with its OWN agent token, so the membership-only gate guaranteed every private-repo run the pool claimed died at `git clone` with a 404 in under a second (this is how #146 silently killed the prod dev loop for two weeks while the public-repo legs kept passing). Repo read is strictly less privilege than the run secrets the allowlist already grants; the admission is scoped to the git fetch surface only — never push, never REST. Metric `clawhub_git_fetch_denied_total{reason}`; tests `tests/git-fetch-authz.test.ts`.

## Git push auth

Git push uses HTTP Basic where the **password (a JWT) is what matters**, and `middleware/auth.ts` `classify()`s the caller into a `PushActor`:
- **Agents** push with username literally `agent-token` + an agent JWT (`{ kind: "agent", agentId }`).
- **Humans** push their own code with username = their handle + a user JWT (`{ kind: "user", userId }`).

Both are accepted and threaded through the same pipeline as a `PushActor`. See `middleware/auth.ts` → `authenticateGitRequest` (sync) or `authenticateGitRequestCached` (async, Redis-backed cache; preferred on hot paths). Merge rights are **role-based and uniform** (v3, docs/redesign-v3.md): WHO may merge is `repo-access.ts:requireMergeRights` (write floor for everyone; a role-holder also needs `change:merge`; role-less = legacy write-admits-merge); WHAT a merge requires is per-repo policy (`evaluateMerge`, evaluated with NO actor-kind input — default policy still requires human code review at high risk / sensitive paths, but that's configuration, not a kind gate). Standing agents still push as agents.

On a successful push, `routes/git-http.ts` snapshots branch heads, proxies to `git http-backend`, and enqueues a `PushJob` (carrying the `PushActor`) on Redis Stream `clawhub:push:received`. A worker (`src/worker.ts` or the in-process worker in `app.ts`) calls `services/post-push-runner.ts` → `services/post-push.ts` to upsert Changes (under a Postgres advisory lock per `(repoId, branch)`), set `refs/changes/<id>`, queue CI runs, fire webhooks, and publish SSE events. An agent push sets `changes.openedByAgentId`; a human push sets `changes.openedByUserId` (exactly one). A human's first push to a new path auto-creates the repo under their own namespace via `ensureRepoForUserPush` (the human-side analogue of `auto-repo.ts`). Pushes to magic refs (`refs/for/<branch>` or `refs/clawhub/for/<branch>`) are admitted server-side: a Change ID is allocated, commits land on `refs/clawhub/changes/<id>`, and the magic ref is deleted. See `services/ref-rewriter.ts`.

If Redis is unreachable at enqueue time, `PushQueue` runs the registered in-process fallback so pushes are never silently dropped.

## Services

| Service | Purpose |
|---------|---------|
| `git.ts` | simple-git wrapper (bare repo ops, trial merge, merge commits). `filesAt` bulk-reads many paths via one `git cat-file --batch` process — use it instead of `fileAt` loops. `stats(ns,repo,ref)` returns on-disk object size / commit count / file count (best-effort, null legs on failure) for the repo-settings metadata card (#33). **`numstat` runs `git diff --numstat -z` and both sides of a rename land in `paths`/`files` (#128)**: rename detection is on by default and the PLAIN format emits a brace-compressed expression (`drizzle/{0001.sql => 0002.sql}`, `{deploy => scripts}/x.sh`) rather than a path, which matches no sensitive glob — so a renamed migration or a moved deploy script defeated the `changedPaths`-driven human-code-review gate, the risk floor, the verify-tier floor and focus synthesis at once. `-z` (not `--no-renames`, which re-expands a pure rename into a full delete+add and inflates the size metric) gives both real paths with true counts: counts attributed to the NEW path, the old path recorded 0/0. Parser splits on NUL, not newline. Tests: `tests/numstat-renames.test.ts`. **`pathOf` is the SINK for the whole git surface and asserts each argument is a single path segment (#138)**: `repositories.name` doubles as an on-disk path component, so a row created with `../victim/repo` (import/fork took the name straight off the request body) pointed at another tenant's directory while every authorization check passed. A `startsWith(basePath)` containment check does NOT catch it — that payload's single `..` cancels the caller's own namespace segment and stays inside the root; the tenant boundary is the DIRECTORY LEVEL. Deliberately weaker than `isSafePathSegment` (it runs on every existing row, so a legacy `My Project` still resolves); boundary validation lives at the creation sites via `namespace.ts:assertSafeRepoName`. Tests: `tests/repo-name-traversal.test.ts`. |
| `git-backend.ts` | CGI proxy to `git http-backend` |
| `change-refs.ts` | `refs/changes/<id>` plumbing (execFile) |
| `auto-repo.ts` | First-push repo creation + permission check. `ensureServiceUserForAgent` (#139) provisions or adopts a service user for a headless agent: email-pinned adoption (only `svc-<agentId>@clawhub.invalid` can be claimed by that agent), reserved-handle refusal for non-system agents, and the back-pointer `agents.service_user_id` keeps legacy rows working. Built on `services/namespace.ts` exports `RESERVED_HANDLES`/`isReservedHandle`/`assertHandleAvailable`. |
| `post-push.ts` | Parse trailers, upsert Change (under advisory lock), link `Closes:`, queue CI for `triggerKind='push'` pipelines only (`ciStatus=skipped` when none), fire events. Branch deletion retracts the branch's unmerged Change. First push to an empty repo adopts the pushed branch as default. **The single `git.numstat` is taken FIRST, above the hard secret gate (#130)**: `changedPaths` (git) drives the gate, the risk engine, the verify-tier floor and the Review Brief, while the author-declared `Scope:` trailer stays ADVISORY (Review-Focus only — #205 moved the agent scope gate onto git facts too: `enforceScope` gets the union of `changedPaths` + declared scope, so `Scope:` can only WIDEN the checked set, and the EFFECTIVE `max(declared, computed)` risk, which is why the call sits below the risk computation; a numstat failure fails a configured path quota CLOSED via `pathsDegraded`). Scanning `scope.slice(0, 40)` meant the pusher wrote the input to its own credential gate. The bulk `filesAt` read is still ONE git process — it covers the declared scope plus the changed paths the scanner will actually look at (`isScannablePath` filters the rest out). |
| `post-push-runner.ts` | Bridge from `PushJob` to `processPush`. Detects magic refs and admits them via `ref-rewriter.ts`. |
| `push-queue.ts` | Redis Streams durable queue (`PushQueue` producer + `PushWorker` consumer group). Fail-open: enqueue runs in-process fallback when Redis is down. |
| `merge-queue.ts` | `MergeQueue` + `MergeWorker` — server-side serialized merges; per-repo lock via `withRepoLock`. |
| `ref-rewriter.ts` | Magic-ref intake (`refs/for/<branch>` → allocate Change ID + write `refs/clawhub/changes/<id>`). |
| `repo-lock.ts` | Redis `SET NX EX` per-repo lock (`withRepoLock`) + Postgres `pg_advisory_xact_lock(hash(repoId|branch))` (`withChangeUpsertLock`). |
| `issue-number.ts` | `insertIssueWithNumber` — the ONE race-safe per-repo `issues.number` allocator (#119). Per-repo advisory lock (`withChangeUpsertLock`, scope `::issue-number`) around read-max + insert, plus a bounded retry on 23505 (the `verify-plan.ts` #81 shape) as the backstop; terminal contention is a typed `ConflictError`, logged + metriced (`clawhub_issue_number_conflict_total`), never a silent drop. Used by `routes/issues.ts` POST, `routes/demo.ts`, `external-sync.ts`, `dep-scan.ts`, and the three importers (which pass `numberHint` to keep their single-`MAX`-per-import fast path and advance from the RETURNED number). `issues_repo_num_uniq` stays the integrity backstop. |
| `token-cache.ts` | Redis-backed JWT verify cache (`verifyTokenCached`). 60s TTL by default; 5s in-process layer in front. |
| `shard-map.ts` | Repo → git-service shard resolution. Rendezvous (HRW) hashing for placement. Returns a synthetic `local` shard when nothing's placed yet. |
| `git-client.ts` | HTTP client for a git-service shard (`init`, `listRefs`, `updateRef`, `deleteRef`, `resolveRef`, `merge`, `fetchPack`, `applyPack`, `mirrorClone`, `forwardGitHttp`). Pooled by `GitClientPool`. |
| `shard-health.ts` | Periodic `/healthz` poller + per-shard circuit breaker (`closed → open → half_open`). |
| `shard-watcher.ts` | Phase 4 failover. Subscribes to Redis keyspace expirations on `clawhub:shard-lease:*` and elects the most-caught-up replica based on `ref_log` tip vs `shard_replication_state.last_seq_applied`. |
| `shard-migration.ts` | Resumable repo migration state machine: `cloning → tailing → cutover → cleanup`. Backed by `repo_migrations`. |
| `replication-tailer.ts` | Replica-side loop that tails `ref_log` for repos hosted here, pulls missing packs from the writer shard, and applies via `update-ref`. Updates `shard_replication_state`. |
| `shard-backup.ts` | S3-backed periodic backups. **A backup carries its OBJECTS and a restore fails LOUDLY (#140)** — it used to upload only a `{refName, sha}` list, so restore `initBare`d an empty repo, every `update-ref` threw "nonexistent object", the per-ref `catch` ate it, and `result:"ok"` was recorded anyway. Now: v2 manifest + `refs.json` + a real `objects.pack` (`git.packObjects`/`applyPack` on the local tier, `GitClient.fetchPack` on a shard), parent-pointer-linked incrementals (`haves` = the parent's ref shas) with a full pack every `CLAWHUB_BACKUP_FULL_EVERY`; `restoreRepo` walks the chain to the last full pack, applies packs oldest-first (sha256-verified), writes refs, repoints `repo_shards`, and THROWS on any missing/corrupt pack, failed ref or legacy v1 manifest (`not_restorable`). `toShardId: "local"` covers the default unsharded deployment. `repo_backups.ref_log_tip` (0076) makes `listDueBackups` measure a real delta instead of every lifetime ref-log row. Tests: `tests/shard-backup.test.ts`. |
| `ref-log.ts` | Phase 4 Postgres-as-WAL writer + reader. Pre-receive hook on a shard POSTs HMAC-signed batches to `/api/v1/internal/ref-log` BEFORE the local apply. |
| `leader-election.ts` | Per-shard Redis lease loop + DB reflection (`git_shards.lease_holder`). |
| `shard-replication.ts` | Legacy: `git push --mirror` to a replica shard (dry-run by default). Superseded by `replication-tailer.ts`; kept while old scripts still call it. |
| `trailer-parser.ts` | `Intent`, `Risk`, `Scope`, `Review-Focus`, `Closes`, `Agent` |
| `focus-parser.ts` | Extract `// REVIEW:` inline comments |
| `risk-engine.ts` | `computeRisk({ declared, changedPaths, additions, deletions, agentPriorRollbacks }) → { risk, reasons }`. Deterministic, explainable (no LLM): path-taxonomy floors → size/no-test/rollback bumps → `max(declared, computed)`. Driven from `post-push.ts` (one `git.numstat`); persisted as `changes.computedRisk` + `riskReasons`. `isGeneratedFile`/`GENERATED_GLOBS` flag lockfiles/snapshots/build output — post-push subtracts their line counts from the SIZE metric (they still count for path floors), so a big generated `package-lock.json` doesn't force HIGH. Lockfiles are NOT in the MEDIUM floor; `package.json` (declares deps) is. **`CONTAINER_TOPOLOGY_GLOBS` (#188)** is the ONE spelling of the container/compose surface (every Dockerfile variant + docker-compose/compose in yml/yaml at any depth), spread into `MEDIUM_FLOOR_GLOBS`, merge-policy's `BASELINE_SENSITIVE_GLOBS` and verify-tier's `TOPOLOGY_GLOBS` so the three can't drift; `*.tf`/`*.tfvars` are `**/`-prefixed. |
| `ci.ts` | CI as config-as-code. `readRepoPipelines` collects `.clawhub/ci/*.yml` (+ `.clawhub/ci.yml`) at a commit (name = `name:` field, else filename); `upsertRepoPipeline`/`syncRepoPipelines` upsert each, deriving `triggerKind`/`triggerConfig` via `parsePipelineTrigger`. Called from `post-push.ts` on default-branch pushes only (same trust model as policy-as-code). Additive: never deletes DB-only pipelines. |
| `merge-policy.ts` | `evaluateMerge({ policy, risk, computedRisk?, scope, reviews, ciStatus, verifiedAttestation? }) → decision` — **NO actor-kind input (v3)**: requirements evaluate identically for humans and agents (`mergeActorIsAgent` + `allowAgentMergeWithoutCi` are gone). Effective risk = `max(risk, computedRisk)`. Reviews carry `basis: behavior\|code\|both`; at/above `codeReviewRequiredAtRisk` (default `high`) or on a forced path, only `code`/`both` human approvals satisfy the gate — behavior-only blocks with `needs_code_review`. v3 knobs: `requireCiRun` (uniform CI strictness — when true, `skipped` blocks EVERY actor; default off, `skipped` passes for everyone) + `sensitiveBaseline` (default true, editable — `BASELINE_SENSITIVE_GLOBS` demoted from non-removable floor to default policy content). Merge RIGHTS live in `repo-access.ts:requireMergeRights` (`change:merge`), not here. **Verified autonomy**: `policy.verifiedAutonomy` (OFF by default, parsed safe in `normalizeMergePolicy`) lets a server-validated `verifiedAttestation` substitute for the one human-code slot, up to `maxRisk`, gated by `allowSensitivePaths` + a configurable `floorGlobs` (`RECOMMENDED_VERIFIED_AUTONOMY_FLOOR_GLOBS` preset) and `agentId !== author` — `satisfiedBasis: "verified"`. |
| `permissions.ts` | v3 RBAC catalog: `PERMISSIONS` (19 keys — repo:read/write/admin, change:read/write/review/merge, issue:*, workflow:read/write/trigger, secrets:write/read_metadata, policy:write, audit:read, memory:read/write, ops:kill), implication table, `PERMISSION_GROUPS` (UI grouping), `DEFAULT_ROLE_DEFS` (Admin/Developer/Reviewer/Auditor). |
| `identities.ts` | v3 identity projection over `users`+`agents` (no table merge): `identityByHandle` (users beat same-named agents, mirroring `namespace.ts`), `listVisibleIdentities` (common-context visibility — shared repo/org only, non-shared = 404). Kill switch `CLAWHUB_DISABLE_IDENTITY_DIRECTORY=1`. |
| `slash-commands.ts` | Thread slash commands: `parseSlashCommand` (LEADING `/dev` `/review` `/verify` `/test` `/scout` `/triage` `/loop` only; trailing text = fenced operator focus) + `handleSlashComment` (humans-only, `workflow:trigger`, repo budget, stamps `triggeredByUserId`, audit `workflow.triggered`). Hooked in `routes/comments.ts` + `routes/issues.ts` POSTs. Kill switch `CLAWHUB_DISABLE_SLASH_COMMANDS=1`. |
| `run-leases.ts` | Coalesce-to-latest dedup for agent dispatches: `agentRunGroup` stamps `ci_runs.concurrencyGroup = agent:<id>:<mode>:<resource>`; `hasLiveRunForVersion` drops a same-`(group, commit)` dispatch (`"duplicate"` reason); `collapseStalePending` skips obsolete pending runs (`superseded`). |
| `workflows.ts` | v4 (docs/redesign-v4.md): WORKFLOWS own instructions + trigger/cadence + optional repo scope (default all); deployments are repo-less (`standing_agents.repo_id` null = global; the target repo resolves at dispatch via `dispatchStandingRun opts.repoId`). **#120 — resolution is also AUTHORIZATION**: the target repo is client-supplied on every dispatch path and `standingRunEnv` derives the run env (memory pack, spec, changed paths) from it, so `dispatchStandingRun` refuses a repo the agent doesn't participate in (`standingAgentReachesRepo` → `repoAccessFor` ≥ review; repo-pinned + system rows short-circuit), `resolveWorkflowRepos` filters the stored `selected` list through the same governed ∩ role-scope filter as `all`, and `createWorkflow`/`updateWorkflow` 403 on `repoIds` the caller doesn't govern. CRUD + `WORKFLOW_TEMPLATES` (slash presets — the old Templates page) + `dispatchWorkflow` (stamps `ci_runs.workflow_id`) + scheduler CAS tick + event-triggered workflows + `workflowActivity` (runs framed as PRODUCED artifacts — reviews, the Change). **#144 — the fan-out cap is a THROUGHPUT knob, never a membership test**: `CLAWHUB_WORKFLOW_FANOUT_CAP` (default 5) bounds one `resolveWorkflowRepos` fan-out (schedule / continuous / manual with no `repoId`). `handleEventForWorkflows` used it as the scope check, so an `all`-scope (DEFAULT) event workflow silently never fired on the owner's 6th+ repo — no run, no log — and stably so, since the ordering key `repositories.updatedAt` is bumped by settings/policy/transfer/Loop writes and by nothing a push or merge does. Membership is now the UNCAPPED `workflowReachesRepo`, a decline logs `workflow_event_repo_out_of_scope`, and `listWorkflowsFor` returns `reachableRepoCount` + `fanoutCap` so the dashboard chip can say "all repos · 8" and "5 of 8 per tick". Routes: `/api/v1/workflows` CRUD+run+activity, `/api/v1/workflow-templates`, deployment PATCH/DELETE//run under `/api/v1/standing-agents/:id`. |
| `public-stats.ts` | #122 — the ONE public-repo-only agent aggregate (`publicAgentStats`/`publicAgentStatsBulk` → `{changesOpened, changesMerged, reviewsSubmitted}` joined to `repositories.isPublic`). **Every public surface filters on repo visibility, aggregates included**: the profile, `badge.svg`, `og.svg`, `agentLeaderboard`, `countStats` and `sitemap.xml` all read from here. The denormalized `agents.stats` counters are an unfiltered private+public lifetime total and are never published. |
| `code-graph.ts` | **Graphify** — the structural CODE graph (`code_graph_nodes/edges`): per-language def/import extraction in the `code-index.ts` style, incremental on default-branch pushes. Per-repo `repositories.graphifyEnabled` (default true); kill switch `CLAWHUB_DISABLE_CODE_GRAPH=1`. NOT the memory graph (`memory_edges` keeps its name). |
| `personal-agent.ts` | `ensurePersonalAgent` — find-or-create the user's ONE default agent (`{handle}-agent`, Developer role, dormant); `ensurePersonalAgentInBackground` fires on register/OAuth first-login (kill switch `CLAWHUB_DISABLE_DEFAULT_PERSONAL_AGENT=1`). |
| `verification.ts` | `recordVerification` (server re-binds run/agent/commit/no-self-verify; computes status from `checks`; upserts `verification_runs` on `(changeId, headCommit)`) + `loadVerifiedAttestation` (success row for the CURRENT head only → stale-after-push is automatic). The non-spoofable anchor for verified autonomy. **Evidence is store-verified (#155)**: `resolveVerifiedEvidence` resolves every claimed `evidence[]` + `checks[].evidenceUrl` against the evidence object store (blob must EXIST at `evidence/<repoId>/<changeId>/<blobId>`, ids keyed by the SERVER, not parsed from the URL) before `evaluateCoverage` sees them — the payload used to be its own proof via a substring test, so a hallucinated URL minted a `ui`-backed success attestation. No store wired ⇒ `ui` fails closed. `evaluateCoverage` stays pure and trusts only the surviving list. |
| `changes.ts` | `ChangeService` — `evaluate()` (loads the head-pinned `verifiedAttestation`), `merge()` (wrapped in `withRepoLock`), `rollback()`, `maybeEnqueueAutoMerge()` (hands-off auto-merge of a verified+mergeable change; needs the injected `MergeQueue`). **Conflicts are re-checked FRESH under the repo lock (#147)**: `changes.hasConflicts` is push-time state, so `mergeLocked` runs `trialMerge` against the CURRENT base right before the git merge (409 with the update-branch affordance) — and the git layer itself fails closed (`git.ts:mergeTreeOrThrow`: merge-tree exits 1 with the CONFLICTED tree on stdout + EMPTY stderr, the exact shape simple-git RESOLVES, so the conflicted tree used to be committed to trunk as a "success"). `reopen()` from abandoned re-queues the on:push CI at the head (#203 — abandon cancelled this head's runs and a cancellation votes pending). |
| `ci-runner.ts` | Runner callback — atomic claim, updates run, recomputes change `ciStatus` (newest run per pipeline votes; **a `skipped` run bearing a cancellation `terminalReason` never votes `success` and blocks as `pending` — #203, run-level `skipped` means exactly "cancelled" since the runner only ever reports running\|success\|failure**), `reapStaleRuns` sweep for zombie runs, `reconcileDeployRuns` (boot + reaper cadence: a merge-group deploy run for the commit THIS API instance is running cannot have failed — severed-report phantom failures flip back to success). **CI concurrency groups**: a run may carry `ci_runs.concurrencyGroup`; a partial unique index (`ci_runs_running_group_uniq`, migration 0035) enforces AT MOST ONE `running` run per group, so the claim (pending→running) for a second run in a group fails with 23505 → treated as "group busy" (409), run stays pending. On terminal/reap, `dispatchNextInGroup` runs the NEWEST pending run in the group and collapses older pending ones to `skipped`. Merge→deploy runs (`changes.ts`) get group `merge:<repoId>` so a repo's deploys serialize newest-first instead of racing on the shared `~/clawhub` checkout. |
| `log-redact.ts` | #142 — best-effort secret masking for CI run output. `redactionPatterns` expands each of the run's secret VALUES into the literals a log realistically carries (raw, `base64(value)`, `base64(user:token)` per ordered pair — the docker-config shape — and the URL-encoded form), longest-first; `redactSecrets`/`redactDeep` replace them with `***` in the log blob and in `stepResults`. Wired at `routes/ci.ts` `POST /runs/:id`, i.e. SERVER-SIDE at the sink, because the runner is a separately-upgraded deployable and the API is the only process that can `unseal()`. Skips <6-char and deny-listed values; an unresolvable secret set stores the output but logs + meters the miss. |
| `llm-pricing.ts` + `routes/llm-gateway.ts` | **ONE admission gate + ONE price for everything the platform's OpenRouter key serves (#143).** `routes/llm-gateway.ts:openRouterAdmission` (catalog lookup → 400 `uncatalogued_model`; org provider allowlist → 400 `org_provider_denied`) is called by `/openai/v1/chat/completions` AND by the `/anthropic/v1/messages` OpenRouter fallback (`viaOpenRouter` — taken when no native Anthropic key is configured), which then forces `body.provider = providerBlock(entry)` + `body.model = modelForRequest(entry)` over the allowlisted body and meters at the catalog price on BOTH the streaming and non-streaming branches. That fallback is what a DEFAULT-configured tenant Loop takes (`standing_agents.llmProvider` defaults to `anthropic`, which selects this gateway), and it used to reach the same key with no catalog gate, no pin and no allowlist — the D8 jurisdiction guarantee, the anti-prompt-injection containment and `organizations.llm_provider_allowlist` were inert for exactly the traffic they exist for. `llm-pricing.resolveModelPrice` now consults `catalogEntry` before the `haiku\|sonnet\|opus\|glm` family substring table, which matches no DeepSeek/Qwen/Llama slug and dumped all of them on the "never undercharge" Sonnet backstop (V4 Flash: $2/$10 vs a real $0.09/$0.18) — and that inflated cost drives `checkPlatformBudget` + `addGlobalSpend`, so the $100 global ceiling tripped ~20× early and 402'd every platform review on the instance. `catalogPriceMicroUsd` computes in the micro domain with the same `toFixed(6)` snap as `priceUsageMicroUsd`, so the two agree exactly. `clawhub_llm_gateway_request_total` carries `protocol` + `via` — the compat route was previously indistinguishable from native Anthropic traffic. |
| `ci-yaml.ts` | CI YAML parser. `parsePipelineTrigger` → `{ kind: push\|merge\|schedule\|event, config }` derived from the `on:` field; persisted to `ci_pipelines.triggerKind` + `triggerConfig` at upsert (routes/ci.ts PUT). |
| `cron.ts` | Dependency-free 5-field cron matcher (UTC). `parseCron` (throws on malformed) + `cronDue(expr, last, now)` — true iff a tick fired in `(last, now]`. Supports `*`, `*/n`, `a-b`, `a,b,c`, exact; Vixie dom/dow OR semantics. |
| `ci-trigger.ts` | Shared enqueue path for schedule/event runs. `enqueueTriggeredRun` reuses the EXACT `ci.run.queued` payload the push path publishes (runner unchanged). Loop guard `shouldEnqueueTriggered` = depth cap (`MAX_TRIGGER_DEPTH=1`) + `(pipeline, commit, triggerEvent)` de-dup. Runs hold a per-run `runnerToken` like push/merge — no new privilege, never merge. |
| `pipeline-scheduler.ts` | `on: schedule` driver. `startPipelineScheduler` (setInterval ~60s, unref'd) → `runSchedulerTick`: finds `triggerKind='schedule'` pipelines, `cronDue` gate (UTC), compare-and-swap claim on `lastScheduledRunAt` (no double-fire across loops/processes), enqueues at default-branch HEAD. Started from `app.ts`. |
| `event-pipeline-trigger.ts` | `on: event` fan-out. `wireEventPipelineTriggers` subscribes to the EventBus; on each event, enqueues matching `triggerKind='event'` pipelines (`triggerConfig.event === e.type`) at default-branch HEAD, depth 1. Guard (a): `ci.*` events never fan out (`isCiOriginatedEvent`). Started from `app.ts`. |
| `token-revocation.ts` | DB-backed revocation checked on token-cache misses: agents must match `token_hash`, users must match `token_version`, and a user with `users.disabled_at` set (SCIM `active:false`, #133) is denied outright. `assertNotDeprovisioned` is the mirror for the sign-in paths that MINT tokens; `assertSignInAllowed` wraps it AND refuses `kind='service'` — the SSO/OAuth doors call the latter so no interactive session is ever minted as a platform namespace (#153). `CONSENT_SOURCES` (`invite_accepted`/`sso_jit`) is the `org_members.source` set an SSO provider may trust to resolve a PRE-EXISTING account cross-tenant — an `admin_added`/`scim` row is NOT consent (#153). |
| `oauth-identity.ts` | OAuth account resolution: provider-ID first, verified email second (links), create last; rotates passwords on takeover-risk links; refuses a deprovisioned account |
| `scim-tokens.ts` | SCIM credentials → the scope they authorize (#133). A per-ORG token (`scim_tokens`, sha256 at rest) confines every SCIM operation to that org's members; the legacy instance-wide `CLAWHUB_SCIM_TOKEN` (single-tenant self-host) is compared CONSTANT-TIME and read at call time, so rotating it needs no restart and unset still fails closed. `createScimToken`/`listScimTokens`/`revokeScimToken` back the org-admin routes; only `resolveScimScope` ever sees the digest. |
| `gdpr.ts` | The ONE deletion cascade. `runDeletionCascade` is the awaitable body: `executeDeletion` runs it DETACHED (self-serve GDPR paths — the `gdpr_requests` row is the outcome record) and `deleteUserAccount` AWAITS it and rethrows on failure. Account deletion has EXACTLY ONE implementation and EVERY door calls it — self-serve GDPR, SCIM `DELETE /Users/:id`, and admin `DELETE /users/:id` (#170) — because a second divergent deletion path is how #104/#106 came back through the SCIM door (#133) and again through the admin door (#170: a bare `db.delete(users)` left live agent tokens + orphaned repos). `grep "delete(users)" src/` must return one hit, inside the cascade. |
| `lfs.ts` | Git LFS object store on disk. **The oid is a PATH SEGMENT and is client-supplied**, so `assertValidOid` (`/^[a-f0-9]{64}$/`) validates it at the route boundary on all four oid-bearing endpoints (`PUT`/`GET`/`verify` + every `objects[]` entry of the batch) BEFORE any DB or filesystem touch — Hono URL-decodes `:oid`, so an unvalidated `%2f` was a real separator and gave arbitrary read/write (#124). `pathFor` independently asserts the resolved path stays under `<baseDir>/lfs` (mirrors `LocalObjectStore.pathFor`), and `writeObject` compares the digest BEFORE `rename()` so a body that doesn't hash to its declared oid never reaches the destination. |
| `secrets.ts` | tweetnacl seal/unseal with `CLAWHUB_SECRETS_KEY` |
| `events.ts` | `EventBus` (Redis Streams + in-process subscribers for SSE) |
| `webhooks-dispatch.ts` | HMAC-signs + POSTs to subscribed repo webhooks |
| `webhook-queue.ts` | Durable deliveries: event-wake for instant dispatch + 5s sweep for retries (CLAWHUB_WEBHOOK_POLL_MS) |
| `repo-resolver.ts` | `resolveNamespace`, `resolveRepo`, `mustResolveRepo` |
| `auth.ts` | JWT sign/verify, bcrypt passwords, sha256 token hashes (legacy bcrypt accepted), random tokens |
| `errors.ts` | `AppError` / `NotFoundError` / `AuthError` / `ForbiddenError` / `ConflictError` / `ValidationError` / `GitError` |

## Routes

All under `/api/v1/...` unless noted:

- `users` — register/login/me.
- `agents` — register (requires a valid user Bearer riding along — the agent is associated to that human at creation; 401 without one unless `CLAWHUB_ALLOW_UNCLAIMED_AGENT_REGISTER=1` for self-host/dev + vitest), me, rotate-token, `POST /personal` (user-only; get-or-create the caller's default personal agent `{handle}-agent` — the same dormant agent auto-created on register by `services/personal-agent.ts`), `DELETE /:id` (user-only; **soft-delete/archive** one of the caller's agents — sets `agents.archivedAt` + revokes the token via a non-matching `tokenHash` sentinel; `GET /` filters out archived. History the agent authored is preserved; not a hard delete. Migration 0034 adds `archived_at`). **v3: the claim flow is REMOVED** — no `POST /claim`, no claim-token rotate, no claim tokens (`agents.claim_token*` columns stay orphaned until a cleanup migration). Both create paths (`POST /agents` + `POST /agents/managed`) now call `assertHandleAvailable` (#139) to gate reserved platform handles (gh-mirror, clawhub-system) and cross-kind collisions (users ∪ agents ∪ orgs); reserved → 400, taken → 409.
- **Agents-ux / RBAC** (`routes/agent-identity.ts`, user-token only): `GET/POST /api/v1/llm-keys` + `DELETE /llm-keys/:id` (sealed BYO key vault, names only returned) + `GET /llm-keys/:id/models` (#72 — selectable models for a vault key, `services/byo-model-catalog.ts`: LIVE-first for anthropic — the provider's own `/v1/models` called with the user's unsealed key, per-key cached, so new Anthropic models appear with no ClawHub change — falling back to the static per-provider catalog (env-overridable via `CLAWHUB_BYO_MODEL_CATALOG`; kill switch `CLAWHUB_DISABLE_BYO_LIVE_MODELS=1`); create/edit validation accepts the UNION of live+static; providers with neither return `[]`, meaning free-text); `GET/POST/PATCH/DELETE /api/v1/access-roles` — v3: roles carry a `permissions: Permission[]` array (`services/permissions.ts`; legacy `{push,review}` shapes translate on read), defaults Admin/Developer/Reviewer/Auditor seeded, `GET /` also returns `permissionGroups` for the UI, and `POST/DELETE /access-roles/:id/assign` assigns/unassigns a role to ANY identity (`role_assignments` — human roles are additive grants, agent roles are ceilings); `POST /api/v1/agents/managed` (the one create flow — local returns the token once; deployed = per-repo standing rows sharing one identity, validate-everything-before-insert so failures leave no orphan; pinning a non-agentic catalog model on an agentic mode → 400 `model_not_agentic`).
- `identities` (`routes/identities.ts`, user token) — v3 People directory: `GET /api/v1/identities?q=` (common-context scoped), `GET /:handle` (profile), `GET /:handle/activity`, `PATCH /self` (displayName/avatar/bio). Kill switch `CLAWHUB_DISABLE_IDENTITY_DIRECTORY=1`.
- `workflow-runs` (`routes/workflow-runs.ts`) — v3 Workflow Runs surface: `GET /api/v1/repos/:ns/:repo/workflow-runs[/:id]` + cross-repo `GET /api/v1/workflow-runs` — agent-origin `ci_runs` presented decoupled from CI (agent identity, `triggeredBy` via `ci_runs.triggered_by_user_id`, composed timeline, metered cost).
- `templates` — `/api/v1/templates` is the v3 alias for `routes/agent-roles.ts` ("Role" now means access control; the legacy agent-role templates are TEMPLATES; DB name unchanged).
- `orgs` — create, list, add members. Also the org-scoped SSO/IdP config and (#133) the **SCIM credential registry** `GET/POST/DELETE /:orgId/scim/tokens` (`routes/sso.ts`, org-admin + Team+ gated; the raw token is returned once).
- `scim` — `/api/v1/scim/v2/*` (#133). Mounted with the genuinely-public routers because it owns its OWN auth: an IdP presents a provisioning bearer, not a ClawHub JWT, so below the wildcard-auth `/api/v1` routers `authMiddleware` 401'd every call before the handler ran (the #101 shape). `active:false` disables (`users.disabled_at` + token_version bump), `active:true` re-enables, an unimplemented op is a 4xx SCIM error validated before anything is applied, and `DELETE` awaits `gdpr.ts:deleteUserAccount`. Every read/write is scoped by `scim-tokens.resolveScimScope`; `service` users are invisible; all four mutations are audited.
- `repos` — list/get/patch (GET `?stats=1` adds a best-effort `stats` object — on-disk size / commit count / file count via `git.stats`, off by default so the hot read path stays a DB-only read; #33), collaborators, transfer, **`DELETE /:ns/:repo`** (full irreversible delete — gated hard: HUMAN user token only (agents 403 `users_only`), repo ADMIN via `resolveRepoForAdmin`, and a typed `{confirm:"<ns>/<repo>"}` body; audited as `repo.deleted` with `repoId:null` so the trail survives the cascade; DB row delete cascades to all `repoId` rows, then `git.remove` clears disk).
- `changes` (mounted under `repos`) — list, get (+ live `behindBase`), diff (`mode=focused|full`), merge, **update-branch** (`POST .../changes/:id/update-branch` `{method:merge|rebase}` — brings the change current with its base via merge-base-in or rebase WITHOUT moving base, re-runs on:push CI; content conflicts → 409 "rebase locally"; `ChangeService.updateBranch` + `git.updateBranchInto` for LOCAL repos, the git-service `UpdateBranch`/`IsAncestor` ops via `git-client` for SHARDED repos — grpc transport falls back to a clear "use HTTP" error), rollback.
- `reviews` (mounted under `repos`) — list, submit.
- `issues` (mounted under `repos`) — CRUD + comments.
- `ci` — public `POST /api/v1/ci/runs/:id` (runner callback) + protected under `repos`: list/put pipelines, list runs. PUT derives `triggerKind` + `triggerConfig` from the YAML `on:` (`push`\|`merge`\|`schedule`\|`event`); rejects `on: schedule` with a missing/invalid `cron:` and `on: event` with no `event:`. The runner callback MASKS the run's secret values out of BOTH output sinks before persisting them (#142, `services/log-redact.ts`) — the log blob and `stepResults[].out/err` are both served at repo `read`, so plaintext there inverted the privilege that `GET /runs/:id/secrets` is so carefully bound by. It also refuses to write the log blob at all unless the caller's `runnerToken` matches.
- `secrets` (mounted under `repos`) — names-only GET, PUT sealed value, DELETE.
- `releases` (mounted under `repos`) — list + create. `changeId` is OPTIONAL (`resolveReleaseTarget`): a release may be cut from a `tag` + optional `commit` (default: default-branch HEAD), so you can tag current main without a Change. When `changeId` IS given it must reference a merged change (old contract).
- `webhooks` (mounted under `repos`) — list/create/delete.
- `standing-agents` (mounted under `repos`) — CRUD + `POST /:id/run` (manual tick). Operator-gated (user with repo write). Attaches a BYO 24/7 agent: sealed agent token + LLM key, a trigger (manual|continuous|schedule|event), runs as a `ci_runs` row (`origin='agent'`). `services/standing-agents.ts` + `services/standing-agent-scheduler.ts`. **The verify tier is resolved from the Change on EVERY dispatch path, re-delivery included (#172)**: `queuedPayload`'s `verifyTier` parameter is REQUIRED (a new call site can't compile without deciding), `republishStalePendingStandingRuns` re-reads `changes.verify_tier` exactly like `dispatchStandingRun`, and the no-tier legacy fallback is `services` (non-privileged), never `dind`. See docs/standing-agents.md.
- `memory` (mounted under `repos`) — agent memory (FIT). Agent token: `GET /memory` (scoped union, ranked), `POST /memory` + `/memory/batch` (write, secret-scanned, idempotent on sourceRunId), `GET /memory/consolidation-candidates`, `DELETE /memory/:id` (invalidate), `POST/GET /memory/:id/edges` (graph edges). User token: `GET /memory` (repo view) + `PATCH /memory/:id` (pin/archive/review) + `GET /memory/graph` (nodes+edges). `services/memory.ts` + `services/memory-index.ts` (6-leg ranker incl. `graph`) + `services/memory-graph.ts` (edges + graph walk) + `services/memory-decay.ts`. See docs/memory.md.
- `social` — star/watch/follow + `GET /repos/:ns/:repo/social` (current-user state + counts).
- `events` — `GET /api/v1/events/stream` SSE. Auth via `?token=` or header (EventSource cannot send headers); mounted before the bare `/api/v1` routers whose `use("*")` auth would shadow it.
- `git-http` — `/:ns/:repo.git/*` (Smart HTTP).
- `code` (mounted under `repos`) — `tree` / `blob` / `readme` / `branches` read-only browsing + `GET /code/graph?symbol=&path=` (the v3 Graphify structural code graph, read access). `tree` has TWO shapes: query-param (`/tree?ref=&path=`, the dashboard's api.ts) and GitHub-style path (`/tree/<ref>/<path...>`, incl. the root cases `/tree/main` and `/tree/main/`). The path form resolves slash-containing branch names greedily against the branch list (`splitRefPath`).
- `fleet` — `GET /api/v1/fleet` (the caller's PERSONAL fleet via `getMyFleet` — their own agents) and `GET /api/v1/fleet?org=<id>` (org fleet, members only). Same `OrgFleet` shape either way; backs the dashboard Fleet tab at any scale ("Solo = N=1").
- **Cross-repo agent aggregates** (`routes/agent-aggregates.ts`, user token): `GET /api/v1/standing-agents` (every standing agent across the caller's governed repos) and `GET /api/v1/memory` (agent memory across them). Each row carries its repo's `repoNs`/`repoName` so the hub routes per-row actions back to the repo-scoped endpoints. Mounted at specific prefixes (not bare `/api/v1`) so the `use("*")` auth can't shadow other routers. **Their repo set is `repo-access.governedRepos(userId)` — NAMESPACE governance, deliberately NOT the read-membership `visibleRepos` the two list surfaces below use (#187)**: both aggregates are cross-repo views onto repo-scoped resources whose own routes gate on namespace ownership and honour no `repo_collaborators` row (`standing-agents.ts:assertOperator`, `memory.ts:assertHumanRepoAccess`), so read-membership here would widen the ACCESS LEVEL, not merely the repo set — handing a reviewer-tier collaborator another owner's deployment config (plaintext `task`, image, egress policy, `lastError`) and their agents' memory through a door the repo-scoped route 403s. A cross-repo aggregate must never be a softer door onto a resource than the repo-scoped route to it.
- `attention` — `GET /api/v1/attention` triage queue over the repos the caller REACHES. Reasons include `awaiting review` (no approvals yet) and `approved — ready to merge`. **Its visible-repo set is `repo-access.visibleRepos(caller, {includeAgentSponsorNamespaces:false})` (#187)** — the same ONE membership implementation `GET /repos` uses. Both hand-rolled the set and queried `repo_collaborators` on the AGENT branch only, so a human holding the direct per-repo grant got an empty queue forever and the dashboard home rendered it as the "nothing needs you" all-clear. The option keeps AGENT callers on exactly the queue they had: `repoAccessFor` also admits an agent to its SPONSOR's namespaces and orgs, so without it a grant-less agent created under a user Bearer inherited its sponsor's whole org here. The `org=`/`repo=` filters still only NARROW. Tests: `tests/repo-visibility-lists.test.ts`.
- `oauth` — `/api/v1/oauth/{providers,:provider/start,:provider/callback}` consumer sign-in (GitHub + Google, env-configured; endpoints overridable for stub testing). Security routes are mounted under both `/api/v1/repos` (canonical) and `/api/v1` (legacy).

## Auth context

`ContextVariableMap.tokenPayload: TokenPayload` is either `{ kind: "user", userId, email, v? }` or `{ kind: "agent", agentId, name }`.

## Rate limit

Redis-backed (falls back to in-memory when Redis is down). Separate buckets: `/api/*` (CLAWHUB_API_RATE_LIMIT, 100/min/IP) and the git surface (CLAWHUB_GIT_RATE_LIMIT, 240/min/IP).

## Tests

`tests/*.test.ts` (vitest, globals enabled). Notable files:
- `trailer-parser.test.ts`, `trailer-focused.test.ts`
- `focus-parser.test.ts`
- `merge-policy.test.ts`
- `git-auth.test.ts` — verifies push auth: agent tokens (`agent-token` username) and user tokens (handle username) both authenticate and classify into the right `PushActor`
- `git-fetch-authz.test.ts` — #146: drives the REAL git-http route against a migrated Postgres (authorization is nothing but membership queries) with `proxyToGitBackend` mocked to a sentinel, so each case asserts both the status AND whether the repo's objects were about to be served. The three leak cases (non-member user, non-member agent, and the `POST git-upload-pack` entry point) FAIL on the pre-fix source; the eight no-regression invariants — owner, collaborator agent, reviewer-tier human grant, anonymous-private 401 + `WWW-Authenticate`, anonymous-public 200, non-member-public 200, missing repo 404 — pass before and after
- `secrets.test.ts` — tweetnacl roundtrip
- `secret-scan.test.ts` — patterns + `scanPushedFiles` (no file cap, truncation is REPORTED, ignored/binary skips are counted); `post-push-secret-scan.test.ts` — drives the real `processPush` over a real temp git repo with a fake `{}`-DB (the rejection path only does two `.limit(1)` selects, so it needs no Postgres) and asserts a credential hidden behind an under-reported `Scope:` trailer, or sitting in the 50th changed file, is still rejected (#130)
- `token-cache.test.ts` — Redis-backed JWT cache (degrades to local cache when Redis is down)
- `repo-lock.test.ts` — advisory-lock key stability + range
- `ref-rewriter.test.ts` — magic-ref parsing (`refs/for/<branch>`)
- `shard-map.test.ts` — HRW placement determinism + distribution
- `cron.test.ts` — 5-field cron matcher (`*/n`, ranges, lists, dom/dow OR, rollover, no double-fire same minute)
- `ci-yaml.test.ts` — `parsePipelineTrigger` (push/merge/schedule/event) + legacy `pipelineTrigger` mapping
- `pipeline-trigger.test.ts` — loop guard (depth cap + de-dup + `ci.*` exclusion), scheduler `cronDue` gating + CAS claim, event repo/type filtering (fake in-memory DB)
- `ci-log-redaction.test.ts` — #142: the masking primitive (raw / base64 / `base64(user:token)` / URL-encoded, deny-list + short-value skips) and the SINK — a direct `POST /api/v1/ci/runs/:id` with a valid `runnerToken` and no client-side masking (an un-upgraded or hostile runner) still reads back `***` from both the log blob and `stepResults`
- `service-handle-takeover.test.ts` — #139: reserved platform handles rejected at both agent-create paths, cross-kind handle collisions rejected, and `ensureServiceUserForAgent` refusing to adopt a service user it did not provision (asserting the squatter's `repoAccessFor` on a shadow repo is `none`)
- `llm-gateway-pin.test.ts` — #143: drives the real `/anthropic/v1/messages` handler with no platform Anthropic key + an OpenRouter key (a fake DB + a mocked upstream, so it runs in the DB-less default suite) and asserts an uncatalogued model 400s without ever reaching upstream, a catalogued one goes up with `providerBlock(entry)` forced over a container-supplied `provider`/`models`/`route`/`transforms`, the org allowlist denies, and the usage row bills the CATALOG price (270k micro-USD, not Sonnet's 12M). Plus a table-driven pass over `openModelCatalog()` asserting no slug falls through to Sonnet and `priceUsageMicroUsd` === `catalogPriceMicroUsd`. Native-Anthropic traffic and the unknown-id Sonnet fail-upward are pinned as invariants (they pass before AND after the fix); the other 10 cases fail on the pre-fix source

## Environment

- `DATABASE_URL` — Postgres (defaults to local `clawhub` user/db).
- `REDIS_URL` — Redis (default `redis://localhost:6379`).
- `JWT_SECRET` — JWT signing key.
- `CLAWHUB_SECRETS_KEY` — 32-byte base64 for libsodium sealing. **Writes reject if unset.**
- `GIT_REPOS_BASE_PATH` — on-disk bare-repo root (default `./data/repos`).
- `PORT` — HTTP port (default 3000).
