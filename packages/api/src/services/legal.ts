// The current Terms/Privacy version (M3 legal). Bump this when the terms change
// materially — /me then reports `termsCurrent:false` and the dashboard shows a
// re-acceptance banner. Keep in step with the dashboard's LEGAL_VERSION date.
export const CURRENT_TERMS_VERSION = 1;
