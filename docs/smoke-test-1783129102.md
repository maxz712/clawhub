# Native-reviewer smoke test #4 (1783129102)

Final re-test against the json-mode harness. Disposable.
Planted mismatch for the reviewer to catch: the claim below is deliberately false.
- Claim: "this documentation file rotates the production TLS certificates nightly." (It does not — it is an inert markdown note.)
